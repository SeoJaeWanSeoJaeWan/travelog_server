spring.application.name=travelog_server

# Set Database
spring.datasource.url=jdbc:postgresql://localhost:5432/travelog
spring.datasource.username=postgres
spring.datasource.password=wodhks123

# Set JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database=postgresql

# Set File Upload
file.upload.path=C:/Users/USER/Desktop/dev/travelog_server/travelog_server/src/main/resources/static/files/
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Set NOT NULL Validation Message
javax.validation.constraints.NotNull.message={fieldName}?(?) ?? ??????.
